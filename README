work
  - adding new tasks (only press Enter on keyboard);
  - delete tasks (press delete button or change task = empty string);
  - change task (double click on task; save changes by click Enter or click
  out side the input).

  
